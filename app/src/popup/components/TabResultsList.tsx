import React from "react";

type Props = {
    tabSearchPhrase: string;
}

export const TabResultsList = (props: Props) => {
    return (
        <div className="tab-search-results-list-container"></div>
    );
}
